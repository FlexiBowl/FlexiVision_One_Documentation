# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Python
__pycache__/
*.pyc
venv/
.venv/
.mypy_cache/
.build_cache/

# Local-only setup guide — never commit (contains internal infra details)
DEPLOY_SETUP.md

# Publisher scratch dirs — survive crashed/interrupted builds, must never be committed
.tmp_manual_build_*/
